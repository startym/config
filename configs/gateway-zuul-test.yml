test: '{cipher}985656c423d60b5b9d7336041ccb03a361913742e5742be0f0b5cbf9b21a2df4'
server:
  port: 9002

spring:
  application:
    name: gateway-zuul
  main:
    allow-bean-definition-overriding: true #允许多个服务引用网关

eureka:
  client:
    service-url:
      defaultZone: http://admin:123@127.0.0.1:6001/eureka/

zuul:
  routes:
    user-provider: /userprovider/**
    user-consumer: /userconsumer/**
  prefix: /gateway
  ignored-patterns: /**/mm/**
  AuthorityFilter:
    pre:
      disable: true

  host:
    socket-timeout-millis: 6000
    connect-timeout-millis: 6000

